pipeline {
    agent {
        dockerfile {
            filename "/root/tagdns/docker/tagdns/Dockerfile"
            label "tagdns"
            args "--network=br0"
        }
    }
    stages("test") {
        stage("start process") {
            steps {
                sh tagdns
            }
        }
    }
}
